{
  "name": "tracekin",
  "version": "0.5.1+build.20260916202500",
  "description": "Local-first activity companion: binds the current project on SessionStart, syncs to Tracekin Cloud by default, shows delivery receipts, and honors `tracekin off` / `tracekin on` / `tracekin status` for the current session only.",
  "author": {
    "name": "Local developer"
  },
  "keywords": ["activity", "hooks", "tracekin"],
  "hooks": "./hooks/hooks.json",
  "mcpServers": "./.mcp.json"
}
