{
  "name": "tracekin",
  "version": "0.7.0+build.20260917025521",
  "description": "Local-first activity companion: binds the project on SessionStart, syncs to Tracekin Cloud by default, and honors `tracekin off` / `tracekin on` / `tracekin status` for the current session only.",
  "mcpServers": {
    "tracekin": {
      "command": "python3",
      "args": ["${extensionPath}${/}plugins${/}tracekin${/}scripts${/}mcp_server.py"]
    }
  }
}
